spring.application.name=firstbite-server

# NCP SMS (production values must be supplied through environment variables)
app.sms.ncp.enabled=${NCP_SMS_ENABLED:false}
app.sms.ncp.access-key=${NCP_ACCESS_KEY:}
app.sms.ncp.secret-key=${NCP_SECRET_KEY:}
app.sms.ncp.service-id=${NCP_SMS_SERVICE_ID:}
app.sms.ncp.from-number=${NCP_SMS_FROM_NUMBER:}

# Replace both values in every non-local environment.
app.security.phone-encryption-key=${PHONE_ENCRYPTION_KEY:firstbite-local-encryption-key-change-me}
app.security.phone-hmac-key=${PHONE_HMAC_KEY:firstbite-local-hmac-key-change-me}

spring.jpa.hibernate.ddl-auto=validate
